require('dotenv').config();const mongoose=require('mongoose');const bcrypt=require('bcryptjs');const User=require('../src/models/User');
const main=async()=>{const[nombre,correo,password]=process.argv.slice(2);if(!nombre||!correo||!password||password.length<8)throw new Error('Uso: npm run create-admin -- "Nombre" correo@dominio.com clave-de-8-caracteres');await mongoose.connect(process.env.MONGO_URI);const existe=await User.findOne({correo:correo.toLowerCase()});if(existe)throw new Error('El correo ya está registrado');await User.create({nombre,correo:correo.toLowerCase(),password:await bcrypt.hash(password,10),rol:'ADMIN'});console.log('Administrador creado correctamente');};main().catch(e=>{console.error(e.message);process.exitCode=1}).finally(()=>mongoose.disconnect());
