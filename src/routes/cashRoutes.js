const r=require('express').Router();const c=require('../controllers/cashController');const {protegerRuta,autorizarRoles}=require('../middlewares/authMiddleware');r.use(protegerRuta,autorizarRoles('ADMIN','CAJERO'));r.get('/actual',c.actual);r.get('/ventas',c.historial);r.post('/abrir',c.abrir);r.post('/cerrar',c.cerrar);r.post('/ventas',c.vender);r.put('/ventas/:id/anular',autorizarRoles('ADMIN'),c.anular);module.exports=r;
